server:
  port: ${DECRED_PORT:8080}

logging:
  level:
    root: DEBUG
    org.springframework.web: DEBUG
    com.palestiner.decred: DEBUG

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${HOST:localhost}:${DECRED_PORT:5432}/${DB_NAME:decred_db}
    username: ${DECRED_DB_USERNAME:postgres}
    password: ${DECRED_DB_PASSWORD:qaz123321999}
